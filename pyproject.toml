[project]
name = "worker-attendance-machines"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pyzk==0.9",
    "requests>=2.28",
]

[dependency-groups]
dev = ["pytest", "pyinstaller"]
